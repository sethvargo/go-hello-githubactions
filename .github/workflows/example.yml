name: Example
on: [push]

jobs:
  my_job:
    runs-on: ubuntu-latest

    steps:
    - name: Mask fruit
      uses: sethvargo/go-hello-githubactions@latest
      with:
        fruit: banana

    - name: Echo fruit
      run: echo "apple, banana, and orange"
